{
  "name": "grunt",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "grunt",  // Script de desenvolvimento local
    "build": "grunt build",  // Build de produção
    "start": "node server.js",  // Caso tenha um servidor customizado
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "16.x"  // Versão recomendada para Vercel
  },
  "devDependencies": {
    "grunt": "^1.6.1",
    "grunt-cli": "^1.5.0",
    "grunt-contrib-clean": "^2.0.1",
    "grunt-contrib-htmlmin": "^3.1.0",
    "grunt-contrib-less": "^3.0.0",
    "grunt-contrib-uglify": "^5.2.2",
    "grunt-contrib-watch": "^1.1.0",
    "grunt-replace": "^2.0.2",
    "rimraf": "^6.0.1"
  }
}
